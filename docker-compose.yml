services:
  api:
    build: ./backend
    env_file: ./backend/.env
    ports:
      - "8000:8000"
    restart: unless-stopped
  web:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - api
    restart: unless-stopped
