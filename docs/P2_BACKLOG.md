# P2 功能完整性待办

本清单记录 2026-08-23 功能闭环审计中暂不进入本轮修复的 P2 项目。处理时应为每项补充“执行结果”级测试，不能只断言入口可见。

- 非查询标签仍显示“复制标签”，点击无动作；应禁用、隐藏或实现对应复制行为。
- “编辑数据库…”目前只展示属性；应改名为“数据库属性…”或实现真正编辑。
- “打印数据库…”目前打印整个浏览器工作台；应提供数据库报告或移除入口。
- 查询结果的 “Profile” 只是重复显示结果 JSON；应接入真实分析信息或改名。
- 左侧树搜索不能完整搜索 Schema，也不能从折叠数据库命中子对象。
- “跳转到记录…”只在当前页内跳转；应支持跨页定位或明确标注“本页”。
- 对象列表中的视图、函数、过程和触发器双击无动作。
- 数据库 SQL 转储只包含基础表，不包含视图、函数、过程、触发器和事件，界面未说明范围。
- 右键子菜单靠近屏幕右侧时不会自动翻转，可能显示到视口外。
